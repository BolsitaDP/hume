import React from 'react';
import { View } from 'react-native';
import { Text } from 'react-native-paper';

export default function HabitDetailScreen() {
  return (
    <View style={{ flex: 1, padding: 16 }}>
      <Text>Habit detail (streak, history, edit)</Text>
    </View>
  );
}
